# Gemini API Key (Required)
GEMINI_API_KEY=your_gemini_api_key_here

# Environment (development/production)
NODE_ENV=production

# Allowed Origins (comma-separated, no spaces)
# For development: http://localhost:3000,http://localhost:8080
# For production: https://your-domain.vercel.app,https://your-custom-domain.com
ALLOWED_ORIGINS=https://your-domain.vercel.app

# Optional: Custom rate limiting (requests per minute)
RATE_LIMIT_RPM=60

# Optional: Maximum text length
MAX_TEXT_LENGTH=5000
